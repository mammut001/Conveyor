# Previous README

This file marks the previous long-form README as preserved in Git history. The primary `README.md` is being optimized for a faster product and architecture overview; detailed implementation documentation remains under `docs/` and in earlier commits.
