python-dotenv==1.0.1
python-telegram-bot==21.10
lark-oapi>=1.4.0
# Google OAuth + Calendar + Contacts (P3.4)
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.130.0
# Optional, used only by scripts/telegram_live_smoke.py for live
# Telegram end-to-end tests (a real user client, not the Bot API).
# Install explicitly when you want to run the live smoke:
#   pip install telethon
# telethon
